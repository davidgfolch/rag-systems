spring:
  application:
    name: rag-webcrawler

server:
  port: ${SERVER_PORT:8085}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics

rag:
  crawler:
    prioritizer: ${CRAWLER_PRIORITIZER:deterministic}